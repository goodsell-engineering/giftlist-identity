<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Identity.Domain\Identity.Domain.csproj" />
    <ProjectReference Include="..\..\src\Identity.Application\Identity.Application.csproj" />
    <!-- GL-57: BCrypt round-tripping is Infrastructure adapter behaviour
         (BCryptPasswordHasher, internal) resolved through the same public
         AddIdentityInfrastructure composition root Identity.Host calls, rather than via a new
         InternalsVisibleTo grant. JWT claims/issuer/audience/expiry are NOT covered here — they
         are proven end to end in Identity.IntegrationTests' SignUp test under a real RS256 key,
         and CONVENTIONS.md "Testing" asks not to unit-test what the integration suite already covers. -->
    <ProjectReference Include="..\..\src\Identity.Infrastructure\Identity.Infrastructure.csproj" />
  </ItemGroup>
</Project>
